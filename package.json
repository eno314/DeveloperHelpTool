{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "deno task dev",
    "build": "deno task build",
    "start:hono": "deno task start:hono",
    "test": "deno test -A",
    "test:watch": "deno test -A --watch",
    "test:coverage": "deno test -A --coverage",
    "lint": "deno lint",
    "lint:fix": "deno fmt && deno lint",
    "compile": "tsc",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "bootstrap": "5.x",
    "hono": "^4.12.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/deno": "^2.5.0",
    "@types/node": "^24.12.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "tmp": "^0.2.5"
  }
}
